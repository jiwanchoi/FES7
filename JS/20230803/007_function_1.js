function add(x, y) {
  return x + y;
}
add(1, 2);

function 더하기(x, y) {
  return x + y;
}
function 빼기(x, y) {
  return x - y;
}
function 곱하기(x, y) {
  return x * y;
}
function 나누기(x, y) {
  return x / y;
}

function slice(x) {
  return x.slice(",");
}
